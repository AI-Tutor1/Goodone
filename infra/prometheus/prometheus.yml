# Prometheus scrape config for Tuitional Finance.

global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: tuitional-backend
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:3002"]

rule_files:
  - /etc/prometheus/alerts.yml
